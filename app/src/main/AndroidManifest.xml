<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.des_ctryptage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.Des_ctryptage">
        <activity android:name=".Delastelle" />
        <activity android:name=".Polybe" />
        <activity android:name=".Transpositionrect" />
        <activity android:name=".Playfair" />
        <activity android:name=".Atbash" />
        <activity android:name=".Cesar" />
        <activity android:name=".Hill" />
        <activity android:name=".Vigenere" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>